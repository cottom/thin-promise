{
  "name": "thin-promise",
  "version": "1.0.1",
  "description": "",
  "main": "./dist/thin-promise.cjs.js",
  "module": "./dist/thin-promise.es.js",
  "browser": "./dist/thin-promise.iife.js",
  "scripts": {
    "pretest": "npm run build",
    "test": "nyc --reporter=html --reporter=text mocha test/promise.spec.js",
    "coverage": "nyc report --reporter=text-lcov | coveralls",
    "build": "rm -rf dist && rollup  --config --environment format:cjs && rollup  --config --environment format:es && rollup  --config --environment format:iife",
    "prepublish": "npm run test"
  },
  "files": [
    "dist"
  ],
  "nyc": {
    "exclude": [
      "node_modules",
      "test"
    ]
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "coveralls": "^3.0.0",
    "husky": "^0.14.3",
    "mocha": "^5.0.1",
    "nyc": "^11.4.1",
    "rollup": "^0.56.2",
    "rollup-plugin-uglify": "^3.0.0",
    "uglifyjs-webpack-plugin": "^1.2.2"
  }
}
